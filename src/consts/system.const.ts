import { QuestionType } from '../enums/question-type.enum';
import { Role } from '../enums/role.enum';
import { SectionType } from '../enums/section-type.enum';

export const ROLE_SYSTEM = Object.values(Role);
export const QUESTION_TYPE_SYSTEM = Object.values(QuestionType);
export const SECTION_TYPE_SYSTEM = Object.values(SectionType);
